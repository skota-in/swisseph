# Makefile for the TypeScript project

# Build and run commands for TypeScript

# Install dependencies
install:
	npm install

# Build the project
build:
	npm run build

# Run the project
run:
	npm start

# Clean the project
clean:
	rm -rf dist
